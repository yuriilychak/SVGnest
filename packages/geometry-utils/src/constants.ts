export const TOL: number = Math.pow(10, -9);

export const NFP_KEY_INDICES: Uint8Array = new Uint8Array([0, 10, 19, 23, 27]);

export const NFP_SHIFT_AMOUNT: number = 26;

export const NFP_INFO_START_INDEX: number = 2;
